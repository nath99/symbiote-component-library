export default defaultOptions;
declare namespace defaultOptions {
    const autohide: boolean;
    const beforeShowDay: any;
    const beforeShowDecade: any;
    const beforeShowMonth: any;
    const beforeShowYear: any;
    const calendarWeeks: boolean;
    const clearBtn: boolean;
    const dateDelimiter: string;
    const datesDisabled: any[];
    const daysOfWeekDisabled: any[];
    const daysOfWeekHighlighted: any[];
    const defaultViewDate: any;
    const disableTouchKeyboard: boolean;
    const format: string;
    const language: string;
    const maxDate: any;
    const maxNumberOfDates: number;
    const maxView: number;
    const minDate: any;
    const nextArrow: string;
    const orientation: string;
    const prevArrow: string;
    const showDaysOfWeek: boolean;
    const showOnFocus: boolean;
    const startView: number;
    const title: string;
    const todayBtn: boolean;
    const todayBtnMode: number;
    const todayHighlight: boolean;
    const weekStart: number;
}
